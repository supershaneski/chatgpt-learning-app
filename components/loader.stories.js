import Loader from './loader'

export default {
  title: 'MyApp/Loader',
  component: Loader,
  tags: ['autodocs'],
  argTypes: {
    onClose: { action: 'onClose' },
  },
}

export const Primary = {
  args: {},
}
