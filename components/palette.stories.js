import Palette from './palette'

export default {
  title: 'MyApp/Palette',
  component: Palette,
  tags: ['autodocs'],
  argTypes: {
    onClick: { action: 'onClick' },
  },
}

export const Primary = {
    args: {},
}
